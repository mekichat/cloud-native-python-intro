# docker build -t my-python-gui-app .   
# docker run -it -p 5000:5000 my-python-gui-app
# we should use -it flag to run the application in Interactive Mode otherwise we will get
# an EOFError: EOF when reading a line